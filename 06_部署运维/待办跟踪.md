# 待办跟踪

## 2026-03-21

- 状态：暂停排查 Notes 页面偶发 `client-side exception`，后续再继续。
- 背景：新笔记内容已可被服务端识别并参与构建，问题聚焦在浏览器侧/运行时一致性排查。
- 当前确认：`music.yoruming.cn` 已完成二级域名接入并启用 HTTPS。
  - HTTP 访问返回 301 跳转 HTTPS
  - HTTPS 访问返回 200
  - Nginx 反代目标为 `127.0.0.1:81`