# 上线状态更新（2026-03-21）

## 1. 结论

项目已完成以下线上动作并验证通过：

- `music.yoruming.cn` 已完成 Nginx 反向代理接入（上游 `127.0.0.1:81`）
- `music.yoruming.cn` 已签发并启用 HTTPS（Let’s Encrypt）
- `http://music.yoruming.cn` 返回 `301` 跳转到 HTTPS
- `https://music.yoruming.cn` 返回 `200`
- 主站进程 `pkh-web` 已执行 `pm2 reload` 与 `pm2 save`
- 主站已重新构建，页脚 ICP 备案号已补充为：`蜀ICP备2026007917号`

## 2. 本次关键证据（服务器侧）

- `curl -I http://127.0.0.1:81` -> `HTTP/1.1 200 OK`
- `sudo certbot --nginx -d music.yoruming.cn --redirect` -> 证书签发成功
- 证书路径：
  - `/etc/letsencrypt/live/music.yoruming.cn/fullchain.pem`
  - `/etc/letsencrypt/live/music.yoruming.cn/privkey.pem`
- 到期时间：`2026-06-19`
- `curl -I http://music.yoruming.cn` -> `301`
- `curl -I https://music.yoruming.cn` -> `200`

## 3. 当前遗留项（暂不阻塞）

- Nginx 警告：`conflicting server name "music.yoruming.cn"`（重复定义）
  - 现状：存在于
    - `/etc/nginx/conf.d/music-web.conf`
    - `/etc/nginx/conf.d/yoruming.conf`
  - 处理策略：本轮按“先不处理”执行，后续统一清理重复 `server_name`。

- 系统告警：`ld.so preload` 无法加载 `$LIB/libonion.so`
  - 现状：不影响本轮证书签发与站点可用性
  - 建议：后续单独做系统层清理

## 4. 回归检查清单（建议）

1. 主站首页、`/projects`、`/notes` 可访问
2. 页脚备案号展示正确
3. 音乐站 HTTPS 打开正常
4. 证书续期演练：`sudo certbot renew --dry-run`

## 5. 下一步建议

- 网络恢复后，拉取最新仓库并统一部署脚本：
  - 支持 `SKIP_GIT_SYNC`（跳过 Git 同步）
  - 支持 `SKIP_NPM_CI`（跳过依赖重装）
- 清理 `music.yoruming.cn` 的重复 Nginx 配置，消除 warning
